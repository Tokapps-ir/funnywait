export default () => ({
  i18n: {
    enabled: true,
    config: {
      // Persian is the default locale; add English as a second locale
      defaultLocale: 'fa',
      locales: ['fa', 'en'],
    },
  },
});
